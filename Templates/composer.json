{
    "name": "${setup_namespace_lowercase}/sitepackage",
    "type": "typo3-cms-extension",
    "description": "Base extension for project ${setup_projectname}",
    "license": [
        "GPL-2.0-or-later"
    ],
    "keywords": [
        "TYPO3 CMS"
    ],
    "version": "1.1.0",
    "require": {
        "typo3/cms-core": "^${setup_typo3version_minor}"
    },
    "autoload": {
        "psr-4": {
            "${setup_namespace}\\\\Sitepackage\\\\": "Classes/"
        }
    },
    "extra": {
        "typo3/cms": {
            "extension-key": "sitepackage"
        }
    }
}